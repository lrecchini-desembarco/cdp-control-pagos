import QaView from "@/components/views/QaView";

// Mismo contenido que /qa (Operaciones también lo usa y sigue entrando por
// ahí); esto es solo un acceso rápido para no salir del panel.
export default function Page() {
  return <QaView />;
}
